[workspace]
members = [
    "endpoint-validator",
    "ws-load-test",
]
resolver = "2"

# [patch.crates-io]
# endpoint-libs = { path = "../endpoint-libs" }

[profile.release]
lto = true
codegen-units = 1

